/* “occupation” and “moonlighting” are read-only and act as constants */
import { occupation, moonlighting } from "./export-bindings.js"
// import { sayMyName } from "../01--dynamic-importing/name.js";

// console.log(`${sayMyName()} works as a ${occupation} during the day and as a ${moonlighting} at night`);
console.log(`Some people work as a ${occupation} during the day and as a ${moonlighting} at night`);
