// The following two variables are only available locally. Prepend “export” to make the variable avaiable outside this file
let firstName = "Walter";
let lastName = "White";

export function sayMyName () {
    return `${firstName} ${lastName}`
}
